# 相对初始版本的改动

对照基线：`29cbb38 initial project`（Paul，2026-09-01）。

初始版本能跑通"打开页面 → Computer Use 点一点 → 让模型描述画面 → 推大屏"这条路。
第一轮改动没有改目标，改的是**结论能不能拿去跟客户对峙**。
第二轮改的是**这东西活在哪**——从一台要 SSH 打隧道的 VM，搬到
「GE 当唯一入口 + Agent Runtime 执行 + Cloud Run 大屏」。

- **第一轮（一 ~ 九节）**：单帧截图 → 连续视频流水线。基线 `29cbb38`。
- **第二轮（十 ~ 十三节）**：上云 + 演示当天暴露的四个 bug。基线 `3cf3664`。

> **`main` 自己也往前走了，不再等于 `29cbb38`。**
> 详见第十三节——那节是给看 PR 的人准备的，**先看它再看别的**。

---

## 一句话

初始版本每 10 秒截 **1 张**图，却让模型描述这 10 秒里发生了什么——25fps 下时序覆盖率
**0.27%**。而 SOP 五条里有四条是"没做某事"（没洗手、没冲洗、没加盖、看手机），
**"没做"这种判断在单帧上不可证伪**。

现在改成连续采集视频、按窗口切片、每片独立送 Gemini 原生视频理解。按 1 FPS 抽帧，
**这段时间里每一秒都有画面被看到**，不再有 10 秒的盲区；成本基本持平，
且每条违规都带一张能打开复核的证据帧。

---

## 一、判定可信度（这是主要目的）

| | 初始版本 | 现在 |
| :--- | :--- | :--- |
| 模型看到的内容 | 每 10s 一张截图 | 连续视频，按 `ANALYSIS_FPS` 抽帧 |
| 1 分钟看到的帧数 | 4–6 帧 | 60 帧 |
| 输出形式 | 自由文本 | pydantic `response_schema` 强制结构化 |
| 违规来源 | 对模型的**思考文本**做关键词匹配 | 模型直接输出 `status` 枚举 |
| 时间戳 | 模型 OCR 进度条猜的 | `video.currentTime` 读出来的 |
| 证据 | 无 | 每条违规按时间点抽帧存 JPG，路径写进记录 |
| SOP 规则 | 硬编码在 prompt 字符串里 | 外置 YAML，改规则不改代码 |

**删掉的最大污染源**：初始版本 `agent_loop.py:143-174` 的
`extract_and_record_segments_from_text`——它对模型的思考文本跑正则，看到"违规/未戴"
就记一条，看到"口罩/手套/红线"就升级成 RED_LINE。模型只要写一句
"检查是否有口罩违规"，就会被记成一条红线违规。

**"看不出来"现在是一个合法答案**：新增 `CANNOT_DETERMINE` 状态。看不全就说看不全，
不硬凑一个 COMPLIANT。

---

## 二、上下文与成本

初始版本把浏览器操作和视频判定**挤在同一个对话循环里**，历史越滚越长。

现在拆成两次不同的模型调用：导航归导航，判定归判定。每个窗口是一次**全新的**
`generate_content`，不带任何历史。

实测（4 个窗口）：每次请求 input token **10470 / 10470 / 10470 / 10470**，
跑 30 分钟和跑 30 秒是同一条平线。这是"几小时录像"这个需求能成立的前提。

配套加了硬护栏：`MAX_WALL_CLOCK_SECONDS`、`MAX_WINDOWS`、token 预算，到点即停并清理
子进程和临时文件。初始版本 `agent_loop.py:564-566` 的 max_turns 默认 100 被静默提到
**100000**，而调用方恰好总是走默认值——等于没有上限。

---

## 三、采集：两条路，自动选

初始版本只有"截图"。现在：

- **Plan A 抓流**：ffmpeg 带着页面 Cookie 直接下载播放器自己的媒体流，无转码损失，
  回放场景可超实时。实测 45 秒素材 **11 秒**拉完。
- **Plan B 录屏**：CDP screencast → ffmpeg 合成 MP4。万能兜底，只能实时速度，
  支持 `PLAYBACK_RATE` 倍速。

`capture/probe.py` 开跑前探测决定走哪条。海康/大华那种 canvas + WASM 解码的页面
没有标准媒体传输，会明确落到 Plan B 并说明原因。

新增 `tests/probe_check.py`：不花钱地问一个新平台走哪条路。

---

## 四、导航：确定性优先，Agent 兜底

初始版本每一步都靠 Computer Use 点。现在：

```
navigator/bilibili.py   确定性 Playwright，写死选择器 —— 快、便宜、可复现
        ↓ 抛异常时
navigator/generic_agent.py   Computer Use 兜底，有明确目标和轮次上限
        ↓ 遇到验证码/登录墙
navigator/base.py HumanGate   挂起 + 大屏推送"需要人工"，人处理完继续
```

`navigator/base.py` 定义了 Protocol，接海康/大华只是新增一个适配器文件，不动主链路。
登录接口已定义、实现留空。

兜底层的成功判定不再看模型自己怎么说：有客观检查就用客观检查（播放头在不在动、
有没有跳到请求的时间点），没有就拿截图再问一次模型要结构化裁决。
原来的判定是 `not reasoning.startswith("BLOCKED")`——模型写"我打不开这个页面"
算成功，于是把登录墙录成了门店录像。

---

## 五、不拿关键词猜开放输入

一轮专项审查，判据是"输入是不是开放的、匹配不上时的默认值会不会是错的"。修了 5 处：

| 位置 | 原来 | 现在 |
| :--- | :--- | :--- |
| 用户那句话（时间段） | 正则匹配几种固定写法 | `intent.py` 交给模型解析；读不准**直接停下来问**，不默认"整段视频" |
| 违规状态 | `"违规" in text` → 否则算合规 | 删掉（结构化输出后是纯污染源） |
| 重试判定 | 错误字符串里搜关键词 | 先看 HTTP code / status 枚举，再退到文本 |
| 时间戳归一化 | 解析失败 → 静默变成 `00:00` | 保留原值 + `timestamp_exact: false` |
| 页面被挡住 | 只认 bilibili 的原话 | `diagnose_block` 拿截图+页面文字问模型 |

实测 `diagnose_block` 在两个关键词表全都认不出的真实页面上：

```
bilibili 404 页  关键词: None → 模型: unavailable「视频不存在或已被删除」
bilibili 登录页  关键词: None → 模型: login「哔哩哔哩登录界面，扫码/密码登录」
```

**没动的**：`.m3u8`/`.flv` 探测（猜错就退到录屏，方向是安全的）、
`seg_00042.mp4` 文件名（是我们自己让 ffmpeg 写的，不是外部输入）。

---

## 六、报告与大屏

- **重复写入**：初始版本 `monitor.py` 和 `monitor_server.py` 各写一遍，
  实测 id 是 `1,1,2,2,3,3`。现在 `store.py` 是唯一落盘入口。
- **顺序**：分析并发跑，窗口按完成先后到达（2 → 1 → 3 是正常的）。
  报告表格、大屏侧边栏、`store.violations()` 三处现在都按**视频时间**排。
- **截断**：报告表格上限 50 行，原来是取"最后 50 个"——顺序既然是乱的，
  "最后 50 个"就是随机 50 个，而且一声不吭。现在取时间最早的 50 个并写明总数。
- **大屏画面**：`capture/preview.py` 独立供给，两种采集模式都有。
  Plan A 下会注明"画面仅供参考，分析进度与这里的播放位置无关"。
- **进度条**：初始版本大屏永远显示"1分钟视频抽检进行中"（硬编码字符串）。
  现在显示本次实际的窗口/重叠/抽帧/画质设置。

---

## 七、安全与工程基线

- `monitor_server` 默认 bind 从 `0.0.0.0` 改 `127.0.0.1`，`/api/event` 加 token 校验。
  **这条比较要紧**：那个接口广播的是含人脸的实时门店画面，原来全网段可读、匿名可写。
- `storage_state`（登录 cookie）落在 `AUTH_STATE_DIR`，已 gitignore，目录 `0700` 文件 `0600`。
- 媒体 URL 的 query 里带鉴权 token，日志里统一脱敏。
- 稽核记录和证据帧含可识别人脸，`checkpoints/` 已 gitignore（初始版本提交过一份，
  本次一并从工作区移除）。
- 新增 `requirements.txt`。初始版本没有任何依赖清单，`start_web.sh` 只装
  `google-adk playwright certifi`，漏了代码实际 import 的 `aiohttp`/`fastapi`/`uvicorn`
  ——全新 venv 首次启动必然 ImportError。
- `.env` 改用 `python-dotenv`（原来的手写解析不处理引号、`export`、行尾注释）。
- GCP project 原来硬编码在三个地方，收进 `config.py`。

---

## 八、测试

初始版本的 `test_local.py` 不是自动化测试（是个手动脚本），已删除。

第一轮做完是 **186 个 pytest**，不碰浏览器、不碰网络、不碰 Vertex AI。
重点压在时间算术上——时间戳错了就会产出一条"自信但错误"的稽核记录，
比没有记录更糟。（第二轮之后是 **380 个**，见第十二节。）

另有三个需要真实环境的手动脚本（不进 CI）：

| 脚本 | 用途 |
| :--- | :--- |
| `tests/manual_e2e.py` | 真浏览器 + 真 Vertex AI 跑通一遍，打印每窗口 token |
| `tests/probe_check.py` | 只探测不花钱：这个平台走 Plan A 还是 Plan B |
| `tests/test_capture_integration.py` | 需要本机 ffmpeg |

---

## 九、文件变化

**删除**

| 文件 | 行数 | 原因 |
| :--- | ---: | :--- |
| `agent_loop.py` | 803 | 拆成 pipeline / navigator / capture / analyzer |
| `computer.py` | 355 | 死代码，动作逻辑在 `agent_loop.py` 里已重复实现一遍 |
| `test_local.py` | 53 | 不是自动化测试 |

**新增**

```
config.py            集中配置          navigator/    确定性导航 + 兜底 + 人工闸门
intent.py            读懂用户那句话     capture/      Plan A / Plan B / 切窗 / 大屏画面
pipeline.py          串起整条流水线     analyzer/     SOP YAML + 结构化输出 + 视频判定
store.py             唯一落盘入口       tests/        186 个单测 + 3 个手动脚本
gcp.py               Vertex 客户端 + 重试分类
browser_actions.py   Computer Use 动作分发（原来在 agent_loop 里）
```

**保留改造**：`agent.py`（129 → 396）、`monitor.py`、`monitor_server.py`、`start_web.sh`

Python 代码 2744 行 → 9947 行（含 2095 行测试）。
（第二轮之后是 20732 行，含 6012 行测试。）

---

# 第二轮：从一台 VM 搬到云上

对照基线：`3cf3664`（第一轮做完那版）。72 个文件，+14128 行。

第一轮做完，这东西只活在一台 GCE VM 上：`adk web` 起在 8000，大屏起在 8080，
人得先 SSH 打隧道才能看。客户要的是另一种形态——**在 Gemini Enterprise 里说一句话**，
其余全都不该看见。

## 十、四个平台事实，是实测出来的，不是文档上写的

这一轮最值钱的东西不是代码，是这几个数。**官方文档里一个都没有**，
全靠部一个只会回 `hello` 的探针容器量出来的（`deploy/phase0/`，
过程和原始回包都留着）。

| 事 | 实测 | 为什么要命 |
| :--- | :--- | :--- |
| GE 那条路能挂多久 | **602 秒** | 我原来推断「约 1 小时」，**错了一个数量级** |
| 一问一答 `:query` | 896 秒 | GE 用不到它 |
| 流式请求里的活 | **900.0 秒被 cancel** | 两个引擎两条流，一秒不差 |
| 甩到后台的游离任务 | **1800 秒跑满，不被 cancel** | 稽核唯一能活下来的写法 |

推论只有一个：**稽核绝不能写成 `await run_audit()`**。900 秒一到整场就没了，
而 GE 在 602 秒就已经报错走人，**根本看不到它是怎么死的**。
所以现在是「立刻返回单号 → 后台跑 → 轮询取」。

还有一条把接口形状改掉了：**GE 只调一个方法**
（`streaming_agent_run_with_events`），把整轮对话原文丢进来。
`classMethods` 里写的 `description` / `parameters` 在这条路上**完全不起作用**。
所以「三个方法当三个工具让 GE 自己挑」这个设想不成立，**分流只能在容器里自己做**。

## 十一、结构上的改动

| | 第一轮 | 现在 |
| :--- | :--- | :--- |
| 入口 | `adk web`，人在 VM 上 | GE，客户在浏览器里 `@门店视频稽核` |
| 一次稽核 | 一个函数从头跑到尾 | `preflight` / `start_audit` / `get_status` 三段 |
| 跑之前 | 直接开跑 | **先探测再复述给人确认**，人不点确认就什么都不发生 |
| 状态 | 进程内存 + 本地 `checkpoints/` | Firestore（按 `user_id` 分子集合）+ GCS |
| SOP | 镜像里的固定路径 | GCS 上按版本号取，**取不到就报错，不回落默认** |
| 大屏 | VM 上 8080，SSH 隧道 | Cloud Run，Google 账号鉴权 |
| 无人观看时 | 照推 | 不推，带宽 0 |

新增的模块：`audit_service.py`（三个方法本身）、`jobs.py`（单子 + 后台执行器）、
`artifacts.py`（证据帧落哪）、`turn.py`（这轮对话该干哪件事）、
`server.py`（容器契约）、`logsetup.py`、`cpuprobe.py`。
部署脚本全在 `deploy/` 下，一律走 ADC + REST——**这台开发 VM 的 gcloud CLI
登录会自己过期且非交互没法重认证**，依赖它的脚本迟早在最不该失败的时候失败。

三条刻意的取舍，都是被具体的坑逼出来的：

- **`preflight` 不保留浏览器上下文。** 两次请求不保证落在同一个容器上。
  代价是多一次导航（几秒），换来无状态、可缩容、重试安全。
- **按 `user_id` + `job_id` 存成子集合**，而不是存平铺再加过滤。
  GE 透传的是调用者真实邮箱，**串号在结构上就不可能**，不靠记得写 where。
- **SOP 在开浏览器之前解析。** 版本号错了不该先花一分钟导航才报。

## 十二、演示当天炸的四个 bug

**四个全都只在「有人正开着大屏」时才触发。** 之前跑的 e2e 没人看，
恰好避开了唯一要紧的那个场景——这是这一轮最值得记住的一条教训。

| bug | 症状 | 根因 | 修法 |
| :--- | :--- | :--- | :--- |
| 容器没日志 | 跑成功的单子也一样没日志 | 根本没有 root handler，兜底 handler 只放 WARNING 以上 | `logsetup.py`：stdout + 一行一个 JSON + `severity` 键 |
| 后台任务挨饿 | 大屏 0.8 fps 幻灯片；一单跑了 62 分钟 | **没有请求在飞的时候 Cloud Run 不给 CPU**，runqueue wait **79–85%** | `/is_busy` 长轮询把响应挂住 + `keepAliveProbe`，等待降到 **0.0%** |
| 没说时间段就默默跑整部片子 | 客户以为查 5 分钟，实际在跑两小时 | 解析不出来时回落默认值 | `turn.py` 现在反问 |
| **弹登录框后大屏画面冻住** | 画面死了，分段巡检还在正常跑 | 见下 | Plan A 也开页面看门狗 |

最后那个值得单独说，因为**它的每一个指标都是好看的**：
Plan A 下 ffmpeg 直接拉媒体地址，浏览器那个页面唯一的作用就是给人看；
bilibili 弹登录框时会**把视频暂停**，而 Chromium 的投屏只在页面重绘时才吐帧。
于是抓帧泵攥着上一帧照发——**帧率满的、字节数恒定的、每一帧一模一样**。
根子三处：Plan A 从来不开页面看门狗（没人关弹窗）、不记播放器位置
（关了也回不到全屏）、画面死了没有任何人吭声。

修的时候有一条是刻意反着写的：**Plan A 下页面卡住不许结束稽核**。
写反了的话 bilibili 一弹框就会把一场 5 分钟的稽核在 01:19 掐掉。单测钉住了这条。

**测试 186 → 380。** 风格没变：不碰浏览器、不碰网络、不碰 Vertex AI。
新增的重点在「云上那些只在生产才发生的事」——单子隔离、后台任务生死、
部署 spec 里探针指向对不对、并发数够不够挂住的探针占一个槽。

## 十二·五、一条能让单子永远转圈的 bug（2026-09-08 修）

客户在 GE 里贴了一个 YouTube 链接，界面显示 **working on it 好几分钟**，
然后就没有然后了。查出来的东西比这个症状严重：

**`page.evaluate` 是 Playwright 里唯一一个没有超时的调用。**
`goto`、`click`、`wait_for_selector` 都有；`evaluate` 没有——
脚本返回一个 promise，它就一直等，页面挂多久它等多久。

而 YouTube 用无头浏览器打开时，`<video>` 元素在，但**没有视频源**
（实测 `readyState=0`、`networkState=0`），
**这种元素上的 `v.play()` 返回一个永远不落地的 promise**——不 resolve 也不 reject。
我们的 `ensure_playing` 里正好写着 `await v.play()`。

三件事叠在一起，就成了单号 `fcfd3c`：

| | |
| :--- | :--- |
| 容器 | 静默挂住 **900 秒**，直到 Agent Runtime 把请求 cancel |
| GE | **602 秒**就已经放弃了，客户看到的是转圈然后没了 |
| Firestore | 单子停在 `probing`，`error` 字段是空的——**没有任何地方记着它死了** |
| 日志 | `Opening https://…` 之后**十五分钟一行都没有** |

三处都补了，因为**任何一处单独补都不够**：

1. **`browser_actions.evaluate_bounded()`**，给每个页面脚本一个 15 秒的期限。
   全仓库 15 处 `page.evaluate` 全部换过来了。
2. **不再 `await` 页面的 promise。** `play()` 和 `requestFullscreen()` 现在都是
   发出去就不管（`p.catch(() => {})`）。播没播成由 `_is_advancing` 看播放头判断——
   这个检查本来就在，等 promise 从头到尾都是多余的。
3. **`NAVIGATION_BUDGET_SECONDS=120`**，罩住整个「开浏览器 → 登录 → 打开 →
   起播 → 跳转 → 探测」。这条**同时管稽核那条路**，不只是 preflight：
   `run()` 用同一个 `_browser_session`，而它是脱离请求的后台任务——
   挂在那儿就是一张永远显示 `running` 的单子，连个会超时的请求都没有。

放在 `_browser_session` 而不是 `preflight` 里，是因为**前两条都要求先猜对哪一步会挂**。
`fcfd3c` 挂住的时候每一步都已经有自己的超时了。

顺带两条：`ensure_playing` 失败时现在会读一下播放器状态，
「`<video>` 没有加载任何视频源」和「广告 / 登录墙」是两种要做的事完全不同的失败，
而**截图上这两者长得一模一样**（都是一块黑的），已有的 `diagnose_block`
分不出来；导航每一步补了一行 INFO 日志，DEBUG 到不了 Cloud Logging。

**修完 YouTube 那条路仍然跑不通，但会在 15 秒内说清楚为什么**：
实测两次分别读到 `Video unavailable` 和 `Sign in to confirm you're not a bot`——
**YouTube 在拦机房 IP**，不是我们代码的问题，容器上同理。
要真支持 YouTube 得另说（登录态或专门的适配器）。

**没动过的那条路复验了**：本地 HLS 靶子仍走 Plan A（`stream`，时长 1200s），
本地 MP4 靶子仍走 Plan B（`screen`），两条的 preflight 结果和修改前逐字一致。
测试 380 → 385。

## 十二·六、Agentic 视频：接上了，但默认没开（2026-09-08）

`media_processing="agentic"` 让模型自己驱动一个视频工具，决定看哪一段、看多细，
而不是我们按 `ANALYSIS_FPS` 均匀喂帧。新增 `MEDIA_PROCESSING=static|agentic`，
默认 `static`；`WINDOW_SECONDS` 默认 15 → **30**。

### 先说实测，因为博客上的说法在我们这儿不成立

全部在 Vertex（`study-project-496907` / `location=global`）上真跑，
用的是**我们自己的 SOP system instruction 和 response schema**，`gemini-3.8-flash`。
每 5 分钟素材的 total token：

| 窗口 | static | agentic |
| ---: | ---: | ---: |
| 30s | 55,590 | 93,360 |
| 60s | 36,920 | 60,600 |
| 120s | 22,874 | 31,146 |
| **300s** | 23,226 | **17,800** |

**agentic 有一笔固定的工具开销，拐点在 120 秒和 300 秒之间。**
30 秒窗口下开 agentic 比 static 贵 68% 且什么也换不到——一个 30 秒的片子没什么可「自己找」的。
所以这两个参数必须配套选：**要么 30s+static，要么 300s+agentic，不要混。**

对照今天线上的形态（15s 窗口，每 5 分钟约 89,000），**300s+agentic 省 80%**。

### 四件文档上没有、只能实测出来的事

1. **真实账单不在 `prompt_token_count` 里，在 `tool_use_prompt_token_count` 里。**
   300 秒窗口实测是 2,723 对 12,671。只读前一个字段会把一场稽核少报 85%，
   而且 `MAX_COST_TOKENS` 这个闸门会守着一个不再反映花销的数。
   `AnalysisOutcome.input_tokens` 现在两个都算。
2. **agentic 忽略 `VideoMetadata` 的 `start_offset` / `end_offset`**（复测确认）。
   给它 130–150s，它照样看完整个文件并报告全片的事件；static 对照只看到 02:10–02:29。
   所以想限定区间只能物理切片——我们本来就在切，不影响，但「整段录像传一次、只问某一段」这条捷径没有。
3. **agentic 忽略 `MEDIA_RESOLUTION`。** 它自己决定看多细。
   这也意味着**基线是什么决定了 agentic 划不划算**：上面那张表的 static 是 low+1fps（云上引擎的实际配置，
   它没设任何分析类环境变量）；换成开发机 `.env` 里的 high+2fps，agentic 30 秒就赢了。
   报成本时要说清楚在跟谁比。
4. **`gemini-3.5-flash` 直接拒绝**：`Video understanding tool is not enabled for this model`。
   这句话既不提是哪个设置要的、也不提哪个模型有。`_explain()` 把它翻译成人话。

### 验的是什么

- 单测 385 → **390**，新增 `TestMediaProcessingSwitch` 5 条：static 带 fps 且不带 agentic 标记 /
  agentic 带标记且**不带**它做不到的 fps / 账单把 tool token 算进去 / 配错模型的报错点名设置和模型 /
  非法值在启动时就被 `validate()` 拦下
- **两条路都真打了 Vertex**（不是假 client），Plan A 抓流和 Plan B 4 倍速录屏各跑：

  | | static + 3.5-flash | agentic + 3.8-flash |
  | :--- | :--- | :--- |
  | Plan A 30s | ✓ 13.3s | ✓ 31.9s |
  | Plan B 30s (4x) | ✓ 18.1s | ✓ 17.0s |
  | Plan A 300s | — | ✓ 24.7s |
  | Plan B 300s (4x) | — | ✓ 25.6s |

  六次都返回了合法的 `WindowResult`（3 条规则各一条 finding）。
  **Plan A 和 Plan B 都支持**——两条路交出来的都是本地一个 MP4，模型不关心字节是抓流来的还是录屏来的。

### 顺带把 `_MAX_INLINE_BYTES` 那个假数字量了

代码里写着 `18MB`，注释说"Vertex 的 inline 上限约 20MB"。**这个 20MB 是猜的，
而且错了一个数量级以上。** 拿真实 MP4 一路往上试，Vertex 给了两个明确的数：

| 试的大小 | 结果 |
| ---: | :--- |
| 9.6 / 16.3 / 18.2 / 20.1 / 23.9 / 33.4 MB | ✅ 全过 |
| 47.7 / 66.8 / 85.8 / 114.5 MB | ✅ 全过 |
| 228.9 MB | ✅ 过（in=7066 token，53s×4 的片子，帧数对得上，不是被悄悄截断） |
| 343.4 MB | ❌ `An inline video/mp4 part is 360028249 bytes, which exceeds the maximum allowed inline size of **256000000** bytes` |
| 457.8 MB | ❌ `Request payload size exceeds the limit: **524288000** bytes`（先撞上整个请求体的 500MB，因为 body 是 base64，约 4/3 倍） |

所以是两条限制：**单个 inline part 256,000,000 字节**，**整个请求 524,288,000 字节**。
起作用的是前者。常数改成 **250MB**，给 prompt 和 schema 留点余量。

**这条直接改了上一节那笔欠账的性质。** 原本写的是「要把 `WINDOW_SECONDS` 调到 300
得先让切片走 GCS」——不用了。250MB / 300s = **6.8 Mbps**，任何码率低于这个数的
门店录像，5 分钟窗口都塞得进 inline。走 GCS 变成一个可选优化（省一次上传的带宽），
不再是前置条件。真碰到超高码率的源，报错那句话照样清楚，不会静默出错。

**GCS 切片上传这件事就此搁置**，等拿到客户真实的 CCTV 控制台、见到真实码率再说。
现在做等于在没有需求的情况下猜实现。

### 部到云上了：`agent:v20`

引擎 `6844158066963775488`，`2026-09-08T14:11Z`。引擎上**没有设任何分析类环境变量**，
所以云上跑的就是代码默认值：`WINDOW_SECONDS=30` / `MEDIA_PROCESSING=static` /
`gemini-3.5-flash` / low / 1fps。**agentic 接上了但默认关着**，要开得显式设环境变量。

云上 A、B 两条各跑了一次真实 e2e（`deploy/agent_runtime/run_audit.py` 打 `:query`）：

| | Plan A（抓流） | Plan B（录屏） |
| :--- | :--- | :--- |
| 单号 | `1c731b` | `389c33` |
| 请求区间 | 05:00 + 120s | 05:00 + 90s |
| `start_audit` 返回 | 0.34s | — |
| 窗口 / 失败 | **5** / 0 | **4** / 0 |
| 覆盖 | 300 → 438 | 301.5 → 412.5 |
| `complete` | True | True |
| 端到端 | 50.8s | 179.3s |
| **输入 token/窗口** | **4564** | **4504** |

**窗口数就是 30 秒生效的证据**：120 秒请求配 30s 窗口 + 3s 重叠（步长 27s）出 5 个窗口，
15 秒窗口的话会是 10 个。

**总 token 降了约三分之一。** 同样 120 秒素材，v19 是 10 窗口 × 3574 = 35,740，
v20 是 5 窗口 × 4564 = 22,820。窗口翻倍但单窗口 token 只涨 28%——
因为 prompt 和 schema 那份固定开销从摊在 15 秒上变成摊在 30 秒上。
**这不是模型变聪明了，是固定成本被摊薄了**，别当成分析质量的改进去汇报。

## 十二·七、大屏在收尾那段又冻住了（job `a8c236`，2026-09-08 修）

Percy 在 GE 里跑 bilibili，弹窗一出来大屏就不动了。**稽核本身是好的**——
`12 窗口 / 0 失败 / 2 违规（1 条红线）`，130.4 秒跑完。冻的是画面。

### 先说清楚不是这次部署改坏的

问的是「怎么老是改着改着把之前的改坏了」，所以先给证据：

- v19 和 v20 的构建源码 tarball 逐文件比过，**`pipeline.py` / `preview.py` /
  `navigator/bilibili.py` 一个字节都没动**。v20 的行为改动只有 `WINDOW_SECONDS` 15→30
- 出问题的那段代码 `git log -L 616,621` 追到 **`3cf3664`（2026-09-03）**，
  就是视频流水线那次重构。`c2fcecf` 加 Plan A 看门狗时沿用了它的生命周期，没碰

但**这确实是我那个「修过并且验过」的补丁上的一个洞**，`c2fcecf` 只覆盖了采集阶段。

### 真正的原因：看门狗死在采集结束那一刻，大屏还要再播一分钟

`run()` 里原来是 `await capture_task` 完就 `watchdog.cancel()`。可是采集停了以后
队列里还压着十几个窗口要分析，而 **`LivePreview` 是跟浏览器一起关的**，
在整个收尾期间照样往大屏推帧。这段时间没有任何人管那个页面。

日志对得严丝合缝：

| 时刻 | 发生了什么 |
| :--- | :--- |
| 15:17:34 | `screencast in 24.9 fps`，正常 |
| **15:17:47** | `[budget_stop] 已采集到请求的终点 06:00` → 看门狗被 cancel |
| 15:17:49 | `screencast in 15.9 fps`，开始掉 |
| 15:18:04 | `screencast in 0.0 fps`，此后一直是 0 |
| 15:18:43 | 12 个窗口跑完，任务结束 |

**画面死在看门狗被取消后的两秒**，冻了 56 秒。chrome 的 CPU 从 125% 掉到 1%——
弹窗把播放按停了，而按设计每 10 秒该去清一次弹窗的那个循环已经不在了。
整段日志里 `playback_recovering`、`网页画面卡在` 一条都没有，
**它不是试了没救回来，是压根没在跑。**

顺带否掉「是不是窗口拉长害的」：同一天 15 秒窗口的 `5cb8b3`，
`budget_stop 11:18:07 → done 11:19:26`，收尾期 **78 秒**，比这次的 56 秒还长。
窗口拉长让这个洞变小了，不是变大。那次只是没赶上弹窗。

### 改了两处

**一、看门狗活到最后一个窗口分析完**（`pipeline.py`）。`watchdog.cancel()` 挪到
`gather(*workers)` 之后；循环条件从 `while not self._stop.is_set()` 改成 `while True`，
只由调用方取消。

配套加了一道闸：**`_stop` 之后 `may_stop` 恒为 False**。不加的话，Plan B 的页面
如果在收尾期正好播完，会去设 `_footage_ends_at`，而 worker 会把队列里所有
起点在它之后的切片直接丢掉——**那些片子是已经采到的、客户要的，会被当成
「视频结束了」静默删掉**。这是让看门狗活得更久唯一真正危险的地方。

**二、像素级检测接到大屏上**（`preview.py` + `pipeline.py`）。
「Chromium 15 秒没送帧」这个判断一直存在，但只写进容器日志——
**而看这张冻住的画面的人在浏览器里**。现在 `LivePreview` 多一个 `on_repaint`
回调，接到 `_note_picture_frozen`，走已有的 `picture_frozen` → 大屏横幅那条路。

两个检测器是互补的，所以都留着：看门狗读播放头，说得清原因，但页面读不到时它就瞎了；
像素检测器不知道原因，却是唯一能看见「页面自称在播、实际什么都没画」的。
a8c236 那次只有后者叫了。它们各自的警告不互相清除（`_repaint_stalled` 那个标志）。

单测 394 → **400**。新增两组：看门狗在 `_stop` 之后仍然清弹窗、仍然推播放，
且不再有资格结束整场；以及重绘检测器只报一次、恢复后重新武装、不清别人的警告。

## 十二·八、云上切到 agentic + 60 秒窗口（`agent:v22`，2026-09-08）

Percy 拍板：换 agentic，窗口改 1 分钟。已部署并跑通真实 e2e。

引擎 `6844158066963775488` → `agent:v22`，`deploy.py` 里新钉三个变量
（`update-spec` 才会推环境变量，`update-image` 不会）：

```
MEDIA_PROCESSING=agentic
WINDOW_SECONDS=60
ANALYSIS_MODEL=gemini-3.8-flash    # agentic 必须，3.5-flash 直接拒
```

第三个不是可选项。3.5-flash 对 agentic 一律回
`Video understanding tool is not enabled for this model`，
而这件事**原来要等第一个窗口跑完才知道**——那时浏览器已经开了、切片已经切了，
然后整场一个窗口一个窗口地失败。现在 `config.validate()` 在启动就拦下来，
并且明写该换成哪个模型。用的是**黑名单不是白名单**：只列实测拒绝过的，
不去猜没试过的模型（`gemini-3.5-flash-lite` 就不拦，有单测钉住）。

### 真实素材上的成本，比合成素材那张表贵 3.4 倍

`config.py` 里那张表是拿演示录像量的，而演示录像几乎是静止的合成图，
**严重偏袒 agentic**。同一段 bilibili 视频，云上实测：

| | 窗口 | 覆盖 | 输入 token | 每秒素材 |
| :--- | :--- | ---: | ---: | ---: |
| `a8c236` 30s static | 12 | 327s | 54,768 | **167 tok/s** |
| `8c5619` 60s agentic | 4 | 231s | 129,982 | **563 tok/s** |

**3.4 倍，不是表里预测的 1.6 倍。** 而且花销不是均摊的——这一场四个窗口分别是
47,018 / 63,694 / 9,642 / 9,628，**两个贵的正是找到东西的那两个**。
这是 agentic 按设计在干活，但也意味着**一场稽核的账没法按时长估**，static 可以。

延迟也跟着变：每窗口 45–155 秒，static 是 ~20 秒。
**一个 60 秒的窗口要 155 秒才出结论，那就不是实时画面了**，
演示时大屏上会有很长一段只有画面没有新结论。

质量有没有变好，**这次证明不了**：没有人工标注真值，两次跑的区间也不一样
（agentic 2 违规 0 红线 / static 2 违规 1 红线）。别拿这个当依据。

### 顺带：十二·七那个看门狗修复，在这一跑里得到了实证

`8c5619` 是 Plan A，`[budget_stop]` 在 15:54:34，任务 15:57:09 才结束——
**收尾期 155 秒**，比 a8c236 那次冻住的 56 秒长得多。整段收尾期里
`screencast in` 一直是 23–25 fps，一次都没掉到 0。
a8c236 是 budget_stop 之后两秒就掉到 0.0 并且再没回来。

### 一件没解释清楚的事

这次是部署后的**全新实例 00020**，按 [[redeploy-wipes-bilibili-session]] 记的规律
应该撞 412，结果日志第一句就是 `Reusing saved session for bilibili.`，直接通了。
`AUTH_STATE_DIR=/tmp/auth`、`.dockerignore` 排除了 `auth/`，
**镜像里没有烤任何 session 文件**（查过）。所以不知道那份 state 是哪来的。
**不据此认为冷启动 412 的问题已经解决**——只是这次没撞上。

## 十二·九、GE 回复里说清用的是哪种分析；新增一条直接读 GCS 的采集路（2026-09-09）

三件事，都是 Percy 同一条消息里提的。

### 1. 「现在跑的到底是 agentic 还是 static」以前只能靠翻 API

这个问题本身就是症状。容器启动时**什么配置都不打印**，所以要回答它，
只能去 `reasoningEngines` 的 `deployment_spec` 里读环境变量。
日志里看不出来，token 数也分不出来（static/60s 和 agentic/60s 差得不够开）。

现在 `server.py` 在 import 时打一行：

```
Config: instance=... processing=agentic window=60s overlap=3s
        model=gemini-3.8-flash capture=auto concurrency=3 ...
```

**下次这个问题在日志第一屏就能回答。**

### 2. 分析方式进 GE 的确认回复

以前 preflight 回的是采集方式（抓流/录屏）和视频总长。但 agentic 和 static
是**两场完全不同的稽核**——3.4 倍成本、每窗口 45–155 秒对 ~20 秒——
而客户看到的都是同一句「开始稽核」。既然是要他确认，就该说清确认的是哪一场：

```
找到视频了：xxx
采集方式：抓流
分析方式：agentic，60 秒一段
视频总长：20:00
```

**只说模式名。** 第一版写的是「agentic（模型自己挑该看的地方，慢一些、贵一些，
看得细）」，Percy 划掉了：那是我们对这个设置的评价，出现在客户当成事实读的
一行里。快慢贵贱他自己会判断，我们负责说清楚跑的是哪一种。

`PreflightResult` 上新增三个字段，用 `default_factory` 从当前配置取，
**不是在构造处传值**：preflight 有四个返回点，其中一个是错误路径，
漏传一处就会在 agentic 的部署上报「static」——那比不报更糟。

### 3. 新增 Plan C：直接读 GCS 上的视频文件

客户给的 SOP 录像是**文件**，有的 100MB+，不嵌在任何网页里。
Percy 在 GE 里直接粘 `gs://桶/路径/文件.mp4`。

| | Plan A `stream` | Plan B `screen` | **Plan C `file`** |
| :--- | :--- | :--- | :--- |
| 输入 | 网页里的媒体流 | 网页画面 | 桶里的一个对象 |
| 要不要浏览器 | 要 | 要 | **不要** |
| 要不要采集 | 要 | 要 | **不要**（见下） |
| 分析单位 | 60 秒一窗口 | 60 秒一窗口 | **整段，一次** |
| 大屏实时画面 | 有 | 有 | **没有** |

#### Plan C 上什么都不采集

第一版是照着 Plan A 写的：ffmpeg 拉字节、切成 60 秒的 MP4、一片一片送模型。
写完之后 Percy 问了一句「视频文件很大而且是完整的一段，还用 ffmpeg 切么，
还是直接把 URI 给模型的 API 就行」。**去量了一次，结果是这版设计是错的。**

拿一段 5 分钟、画面上烧了时间码的视频，放在桶里，要模型看 240–300 秒这一段，
`video_metadata` 里带上 start/end offset，两种模式各跑一次：

| `MEDIA_PROCESSING` | 模型报的区间 | prompt token | 总 token |
| :--- | :--- | ---: | ---: |
| `static` | `起=00:09:00 止=00:09:59`（正好是要的那段） | 4002 | — |
| **`agentic`** | **`起=00:05:00 止=00:10:00`（整个文件）** | 280 | 12087 |

**agentic 直接忽略了偏移量，不报错、不告警。** 也就是说在 agentic 下切窗口，
每一个「窗口」都在悄悄重读整个文件——N 倍的钱，外加一堆和报告里写的时刻
对不上的时间戳。这不是「浪费」，是**错**。

顺带的成本对照：整段 agentic 跑完那 5 分钟是 **12,087** token；
同一段按之前实测的 563 tok/s 切成 agentic 窗口，约 **169,000**。
（材料是合成的静态画面，天然对 agentic 有利，这个倍数别当通用结论。）

所以 Plan C 现在的形状是 Percy 定的那个：

> 放到 GCS 上的文件如果真的要切分那就在落到 GCS 之前就切分了，
> 这样到模型的时候也不要拉数据流了，直接读取 URI 即可。

**对象地址直接交给 Vertex，字节从头到尾没进过容器。** 没有下载、
没有切片、没有上传、没有临时文件要删。`WholeFileProducer` 吐一个
`whole_video=True` 的 `Clip`（带 `uri` 不带 `path`）然后就结束了，
下游的队列、worker、store、大屏事件一行没动。

`Clip` 因此长出了第二种「footage 在哪」的说法，并且**在
`__post_init__` 里强制二选一**：两个都填意味着两个读者可能读到不同的答案，
两个都不填意味着 analyzer 要到发请求那一刻才发现没东西可发。

**时间段这次不生效，而且要在客户点确认之前说出来**：

```
采集方式：直接读文件（GCS）
分析方式：agentic，整段视频一次看完，不切片
要稽核：整段视频，从头看到尾（你说的 05:00 - 07:00 这次用不上）
```

同理，`_coverage` 在这条路上**不拿覆盖区间去和请求的终点比**——
比了就会把一场看完了 30 分钟的稽核报成「只覆盖到 30:00，请求的是到 12:00」，
而覆盖报告可以保守，唯独不能往少了说。preflight 里那两个跨度检查也一起跳过：
3 分钟的视频要求从 05:00 开始，在 Plan A 上是「没东西可查」，
在这里整整 3 分钟照看不误，拒了就是白拒。

#### ffmpeg 还在，但只剩两件小事

- **preflight 读时长**（ffprobe）
- **切证据帧**（`frame_at`）

两件都是几百 KB 的 byte-range 读，跟文件多大没关系。走 GCS 的 JSON API
（`.../storage/v1/b/<桶>/o/<对象>?alt=media`，`-headers` 带 access token）
不走 XML API，是因为对象名必须整体 percent-encode，**斜杠也要编**。

`frame_at` **每次自己现铸 header**，不复用采集时那份：
一场跑很久的稽核，最后一条违规要截图的时候，开场那个 token 多半已经过期了，
而证据帧是最不该丢的东西。

**preflight 失败必须说人话。** `probe_stream` 原来把 ffprobe 的 stderr 吞掉了，
因为 Plan A 探不到就退回录屏，没人需要知道原因。Plan C 没有退路。
新增 `probe_stream_verbose`，403 翻译成
「没有权限读…需要给稽核服务账号在这个桶上加 `roles/storage.objectViewer`」，
404 翻译成「这个文件不存在，检查路径和大小写（对象名区分大小写）」。

**入口两处都认 `gs://`**：`turn.py`（GE 那条路）和 `intent.py`（本地兜底）。
`from_fields` 会**当场**校验 `gs://` 的形状——`gs://桶`（只给了桶没给文件）
在客户还在对话里的时候就被挡下来，而不是一分钟后回一句「打不开这个视频」。

顺带修掉一个一直都在的 bug：`_URL_RE` 是 `\S+`，而**中文逗号后面不空格**，
所以「稽核 <网址>，从 05:00 开始」里捕到的地址是 `<网址>，从`。
现在中文标点直接排除在匹配之外（**只排标点，不排汉字**——
`gs://桶/开店/v3.mp4` 是客户的桶实际长的样子）。

### 大屏在 Plan C 上不出现

没有网页可以直播，也没有一个个窗口的判定可以看——整段视频是一次请求。
所以 GE 的回复里**这条路不再给大屏链接**：

> 好，开始稽核了，单号 `xxxxxx`。
> 这一步要跑一段时间，你随时回「好了吗」查进度，跑完我把报告给你。

中间试过在大屏上挂一句「本次没有实时画面」的说明。删掉了：
点进去看到一个空屏，正是前两次演示翻车时被读成「整个东西死了」的画面，
配一句道歉也还是空屏。**不给这条路本来就没有的东西，比给了再解释干净。**

## 十二·十、大屏上写死的三句话；一句「GE 好慢」查不动的日志（2026-09-09）

都很小，但每一条都是「线上跑的东西和页面上写的东西对不上」。

### 1. 页脚在替一个不存在的产品打广告

页脚原文是「Google Cloud Vertex AI · Gemini 2.5 Computer Use」。这句话是某次
写页面的时候手打进去的，**从此再没有任何东西会让它变**：模型早就不是 Computer
Use 了，而 2026-09-09 去查文档发现连产品名也不在了——文档现在挂在
`/gemini-enterprise-agent-platform/` 底下，标题是 **Gemini Enterprise Agent
Platform**，正文简称 Agent Platform，「Vertex AI Agent Engine」一个字都找不到。
我们部署的 Agent Runtime 是它底下的一个组件，和 Sessions / Memory Bank 并列。

> 我第一版只改了模型名，理由是「Vertex AI 这半句是真的」。**这个判断是错的**，
> 半句也烂了。两半烂的方式一模一样：**都是被人往页面里打过一次，然后就没人管了。**

现在页脚整句从运行时配置拼：

```
Gemini Enterprise Agent Platform · 分析 gemini-3.8-flash（agentic，60 秒一段） · 导航兜底 gemini-3.5-flash
```

产品名是这里**唯一一个没有配置字段能推出来的字符串**，所以单独拎成
`_PLATFORM`，边上留了日期和文档地址，并且用单测把它和页面里那份钉在一起。
「Vertex」在代码注释里还留着——那儿指的是 `aiplatform.googleapis.com` 这个
**线上还在用的接口名**，不是产品名。

单测断言的是「服务出去的字节里不许出现死掉的名字」，两次被我自己写的 HTML
注释绊倒（一次 `Vertex AI`，一次 `ADK Web`）。**注释也会随页面发到浏览器，
view-source 看得见。** 正确的修法是改注释，不是放宽断言。

### 2. 违规列表空着的时候不该发绿票

原文是「✅ 未发现不符合 SOP 标准的违规行为」。那是**结论**，而这个面板是
**列表**。空列表同样覆盖「还没开始跑」和「模型说这段看不清」——这套部署刚
报过「10 项检查中 5 项无法判定」的片子，在它上面盖个绿勾就是假的。改成
「暂无违规记录。」

**这条第一次只改了一半**：静态 HTML 改了，`renderViolations()` 里那份没改，
而渲染器在页面打开一秒内就把静态那份覆盖掉了——**改了等于没改，而且看不出来**。
是 Percy 问「第三个在哪能看到」才发现的。现在单测直接数字符串出现次数（必须
是 2），出现第三份的时候会当场失败。

### 3. 一句「GE 答得好慢」查不动

Percy 报：单 `ad30c9` 还在跑的时候在 GE 问「好了么」，等了很久才回；跑完再问，
秒回。

**查不了。** 容器只记「这一轮到了」，不记「这一轮答完了」，所以日志里根本没有
「答一轮花了多久」这个数。只能真跑两场任务，在外面掐表：

| 条件 | 一轮 GE 形状的对话 |
| :--- | :--- |
| Plan C，容器基本闲着（19 次采样） | 2.22 – 5.81s，**任务在跑和跑完了没有区别** |
| Plan A bilibili 录着 + 大屏真开着（收 1162 帧预览） | 2.26 – 3.52s；唯一一次 9.54s 是那轮要现算最终报告 |
| `:query` 直调 `get_status` | 0.19 – 0.29s，全程如此 |

**决定性的证据在 Percy 那单自己的日志里**：他收到的回答是「已分析 0 个窗口」，
而这句话只有在 12:01:32 之前成立；那轮请求到容器的时间是 12:00:22。
**答案在当时就已经算好并发出去了。** 慢的是发出去之后那一段，在 GE 里面，
我们看不见也够不着。

补的是那个盲区，不是延迟：`serve_turn` 拆成一层计时壳 + `_serve_turn`，
结束时打一行

```
turn done session=9776... in 2.31s (first sentence 0.84s, 3 sentences)
```

`session` 是因为 `containerConcurrency=2`，两轮会交叠；**首句和总时长分开报**，
是因为这是流式，客户从第一句开始读——二十秒说完但两秒开口，和二十秒不吭声，
是两种完全不同的抱怨。写成 `finally`，炸掉的那轮也记得下来（单测覆盖了这条）。

### 4. 一条断了行的粘贴，差点把引擎部到错的镜像上

`build_image.py` 跑完打的提示是两行带反斜杠续行的。从一个终端复制到另一个终端，
**续行没了**：`ENGINE_IMAGE=...` 被当成独立命令跑掉，变量没活到下一行，
`deploy.py update-image` 一个参数都没拿到。这次是崩在 `IndexError` 上，
所以看得见；**要是第二行自己跑起来了，引擎就会被静默地部成默认标签**，
而标签就是标签，事后从哪儿都看不出来。

三处改：提示打一行；`ENGINE_IMAGE` 收短标签（`v29` 自动补全成完整地址，
**空字符串也挡掉**，否则构出个没标签的 `agent:`，会在很远的地方才炸）；
引擎 id 同样收短形式，少给参数报的是人话不是 `IndexError`——而且那句话里
明写「检查你那条命令是不是被折行了」，因为人就是这么走到这儿的。

**故意不设默认引擎**：这个项目里有两个，猜一个来打补丁不是帮忙。

单测 470 → 478。

## 十二·十一、这套测试有 11 个是靠一个没进 git 的文件才过的（2026-09-09）

代码推到第二个远端（`percy-han/cctv-audit`）之后跑了一次 `./check.sh --deep`，
它把仓库重新 clone 出来、在 clone 里跑测试——**11 failed / 468 passed**，
全是同一句：

```
ValueError: Invalid configuration:
  - GOOGLE_CLOUD_PROJECT is not set (no default is assumed).
```

这 11 个测试会真的构造 `AuditPipeline`，而 `__init__`（`pipeline.py:268`）
先跑 `config.validate()`，没有项目就拒绝建。项目从哪来的？
`computer_use_agent/.env`——**那个文件在 `.gitignore` 里**，因为它是某个人的本机设置。

所以这套测试在这台机器上全绿、在任何别的地方全红，**而且这样过了六天没人发现**。
原因是 `./check.sh` 在**当前目录**跑测试，`.env` 就在旁边；只有 `--deep` 是站在
「别人 clone 下来会看到什么」的位置上看的。它头一回真派上用场，就是在代码
第一次离开这台机器的那天。

### 修法：`tests/conftest.py` 里钉死一个假项目号

```python
os.environ["GCP_PROJECT"] = "test-project-not-a-real-one"
```

三个刻意的选择：

- **写死一个假值，不是去读 `.env` 补默认。** 一套跑起来结果取决于谁的 `.env`
  的测试，测的不是代码，是那台笔记本。值明显是假的，将来真有测试去连云，
  这个 id 就是让它当场暴露的东西。
- **`GCP_PROJECT` 而不是 `GOOGLE_CLOUD_PROJECT`。** 前者在 `config.py:144` 里优先，
  所以它能盖过一份设了后者的 `.env`。
- **import 期赋值，不是 fixture。** `config` 是模块级的冻结 dataclass
  （`config.py:439` 的 `config = Config()`），collection 一 import
  `computer_use_agent.config` 就定型了，fixture 那时候已经晚了。

代价是「缺项目就报错」这条分支再没别的测试走得到，所以在
`TestWhichProjectWeAreIn` 里补了一条直接断言 `Config(gcp_project="").validate()`
的——那条分支是有用的：没有它，配错的部署会先建起 pipeline，
然后在很远的地方以别的面目炸掉。

### 顺带：`--deep` 自己也在喊狼来了

同一次运行还报了 `Only in deploy/demovideo: assets`。那不是差异，
是 `check.sh` 里手抄的一份 `.gitignore` 副本**没跟上**——
`deploy/demovideo/assets/` 后来加进 `.gitignore` 了，那份副本没加。
现在排除列表**在运行时从 `.gitignore` 读出来**，不再有第二份。
一个每次都误报的检查，等于没有这个检查。

单测 478 → 480。

## 十三、和 `main` 现在那版的关系（看 PR 先看这节）

**`main` 已经不是 `29cbb38` 了。** 它上面有三个我们分支上没有的提交，
其中 `5bc2cd2 Add GE and Cloud Run deployment architecture`（Paul，2026-09-03）
**和这一轮做的是同一件事**——两条线并行长出了各自的上云方案。

所以合并**不是快进**，会在这几处碰上：

| 文件 | `main` 上的 | 这个分支上的 |
| :--- | :--- | :--- |
| `Dockerfile` | 31 行 | 装 Chromium + ffmpeg，跑容器契约 |
| `.dockerignore` / `requirements.txt` | 有 | 都改过 |
| `deploy_agent_engine.py` / `deploy_cloud_run.sh` / `openapi.yaml` | 有 | 换成 `deploy/` 下按用途分的脚本 |
| `DEPLOYMENT_GUIDE.md` | 311 行 | `STATUS.md` / `TESTING.md` / 各 `deploy/*/README.md` |

**哪套方案留下来，是人要拍的板，不是能靠合并工具解决的。**
这个分支这套的依据是第十节那四个实测数字——尤其是「602 秒」和
「流式里的活 900 秒被 cancel」，它们决定了 `start_audit` 必须立刻返回。
**任何一套把稽核放在请求里 `await` 的写法都活不过 15 分钟**，
这一条建议先拿去对一下 `deploy_agent_engine.py`。

另外两件顺手发现、需要有人决定的（**我不单方面动别人分支上的东西**）：

- `main` 上提交了 `monitor_server.log`（20487 行）和
  `computer_use_agent/checkpoints/audit_records.jsonl`（28 条稽核记录）。
  这个分支的 `.gitignore` 把 `checkpoints/` 整个挡掉了，理由写在里面：
  **稽核记录和证据帧涉及可识别的人**。`main` 上没有证据帧 JPG，只有记录和日志。
- 仓库现在是 private，所以上面这条不是外泄，是**要不要留在 git 历史里**的问题。

---

## 已知未完成

- **Region**：统一用 `global`，未做区域端点验证。意味着请求由 Google 全球负载均衡
  按容量路由，**无法控制落在哪个地理区域处理**。带客户做合规确认时若不可接受，
  退路是视频理解层改用有区域端点的普通 Gemini 模型——分层设计天然留了这条路，
  主链路不受影响。
- **登录**：接口已定义、实现留空，等真实客户平台。不打算硬刚滑块验证码
  （现代滑块查的是浏览器指纹不是拖动轨迹），走"会话复用 + 持久化 Profile +
  人工闸门 + 申请专用账号"。
- **`WINDOW_SECONDS=600`** 会超过 ~18–20 MB 的 inline 上传上限，尚无 Files API 兜底。
- **召回率/假阳性率未量化**。需要一段人工标注真值的真实门店录像，
  分别跑初始版本和现在这版对比——这是唯一能对客户量化说明收益的实验。
- **两级筛查（Phase 2）**：门店视频绝大部分时间无事发生。低画质粗筛 +
  命中才精判，成本可再压一个数量级。单级链路先跑通。

第二轮新欠下的：

- **云上的容器进不去 bilibili。** 登录态 `auth/bilibili.json` 被
  `.dockerignore` / `.gcloudignore` 挡在镜像外（**这是对的**，里面是活的会话 cookie），
  容器里那个 `AUTH_STATE_DIR` 是空的。要么挂 Secret Manager，要么演示走本地。
  **bilibili 那场验收是在开发 VM 上本地跑的，不是云上**——代码同一份，但这个区别要说清楚。
- **画面冻住这件事不落库。** 只进容器日志和大屏动作栏，
  Firestore 单子、`get_status`、报告里都没有。
  客户在 GE 里问进度，看到的还是「已分析 N 个窗口」，一个字都不会提画面停过。
- **有一个窗口跑了 250.7 秒**（其余 24 个都在 15–25 秒）。日志里没重试没报错，
  就是那次模型调用自己慢。不影响结果，但把整场墙上时间拖长了三分半。没查。
- **每窗口 token 从 10470 掉到 ~3560，没解释清楚。** 同一套代码、同一份 SOP，
  差别在素材。最可能是合成素材信息量低，但**没验证，别当成优化成果去汇报**。
- **GE 界面里从头到尾走一遍还没做**，这一步只能由人做。
  必须 `@门店视频稽核`，不 @ 会被 GE 自带助手接走，而且它答得像模像样。

Plan C（十二·九）欠下的：

- ~~只有单测，没跑过真的 `gs://`。~~ **2026-09-09 云上跑通了一次**（v23，单号
  `ad345a`，20 分钟的桶文件）：`start_audit` 0.45s 返回、44.2s 出报告、
  1 个窗口 0 失败、`00:00 - 20:00` 全覆盖、输入 9799 token。
  封面帧从桶里切出来了（10646 字节的真 JPEG），**ffmpeg 按字节区间读 GCS 这段是通的**。
- ~~**`frame_at`（违规证据截图）还是没验过。**~~ **2026-09-09 验了**（v24，单号
  `292712`，客户那段 5 分 02 秒的真实门店录像）：判出 4 条红线违规，
  4 张证据帧全部从桶里切出来落到 `audits/evidence/292712/`，95–101 KB 的真 JPEG。
  其中一张下载看过，画面和判定（员工在操作区看手机）对得上——
  **文件存在只能证明切成功了，证明不了切在对的时刻**，所以这一步是必要的。
  上一次（`ad345a`）验不了是因为合成色块素材一条违规都没有，函数没被调到。
- **Vertex 一次能吃多长的视频，仍然不知道。** 限制是时长不是文件大小，
  而这条路的整个前提就是「一次给一整段」。20 分钟已经证明可以，
  **再拿客户那个真正的大文件探一次**，这是 Plan C 上线前最该做的一件事。
- **桶上的授权**：视频放 `study-project-496907-cctv-audit` 的话不用再授——
  引擎已经在从这个桶读 SOP、往里写证据帧。换别的桶就要给稽核服务账号
  加 `roles/storage.objectViewer`，写进客户的部署清单。
- **Plan C 上大屏什么都不显示**，GE 也不再给链接。证据截图照常。
- **ffprobe 读不出时长的话，报告里的时刻就是模型自己说的数**，
  没有上界可以夹。夹了会更糟（全被压到 00:00），所以留着不夹，
  但这种情况下的时间戳没人核过。

进行中的状态、单号、版本号这类会天天变的东西在 `STATUS.md`，不在这里。
