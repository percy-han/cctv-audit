# 当前状态 — 2026-09-08

上一版（09-04）末尾留了一个「大屏画面到得比配的慢，原因没定论」。
**原因找到了，是平台层的事，不是我们代码慢。修了，云上有实测。**
下面每条都有出处。

## 一句话

**大屏卡顿的根因是 Cloud Run 在没有请求在飞的时候不给容器 CPU——
而我们的稽核恰恰是脱离请求跑的。用 `keepAliveProbe` + 长挂的 `/is_busy` 修掉之后，
观看端从 0.9–1.4 fps 变成 11.9 fps、零次超过 1 秒的卡顿，整场稽核从 233–498s 变成 86s。
bilibili 也跑通了，你报的「弹登录框之后大屏不动了」也修了，见最后两节。**

---

## 线上现在跑的是什么

| 组件 | 位置 | 镜像 / 版本 |
| :--- | :--- | :--- |
| 稽核引擎 | Agent Runtime `.../reasoningEngines/6844158066963775488` | **`agent:v31`**（2026-09-09，实例 `...-00025-5sq`） |
| 大屏 | Cloud Run `cctv-monitor`（us-central1，`minScale=maxScale=1`） | **`agent:v31`**（revision `cctv-monitor-00012-hzf`） |
| 演示视频 | Cloud Run `cctv-demo-video` | — |
| GE agent | 应用 `cctv-audit` 下 `agents/16091433261218097511`「门店视频稽核」 | — |

> 上一版这张表把大屏写成 `v17`，**是错的，它当时还停在 `v14`**。
> 那几版之间大屏自己的代码没动过，所以量出来的数不受影响，但表写错了就是写错了。
>
> **2026-09-09 又踩了同一件事**：引擎部到 v23 的时候大屏没跟着部，
> 于是大屏在 v18 上停了五个版本。`deploy/dashboard/deploy.py` 的开头
> 就写着这两个共用一个镜像、「已经飘开过五个版本一次」——现在是第二次。
> **改了 `monitor.py` 就要单独部一次大屏，改了别的也建议一起部**，
> 两边同号才看得出线上跑的是哪一份代码。

### 2026-09-09 这一天连着部了四次，最后收在 v31

| 版本 | 部到哪 | 改了什么 |
| :--- | :--- | :--- |
| `v28` | 大屏 | 页脚不再写死产品名和模型名，改从运行时配置拼 |
| `v29` | 引擎 | 同上（引擎侧靠 `engine_label()` 把这句发给大屏） |
| `v30` | 大屏 | 违规列表空的时候不再打绿勾，改成「暂无违规记录。」 |
| **`v31`** | **两边** | `serve_turn` 加计时日志；把上面那些补齐，两边号码对回同一个 |

**中间有一段两边号码不一样（引擎 v29 / 大屏 v30），已经收掉了。**
当时的理由是「v30 只有大屏那半边的改动」——听着合理，但这正是这份文档上面
两段在骂的同一件事，只不过这次差的版本少。**不要再留这种状态。**

#### 怎么核「线上跑的到底是不是最新的」

标签和时间都不算数，要比字节。两个办法，这次都用了：

1. **比构建源码。** `build_image.py` 每次都把打包好的工作树传到
   `gs://<桶>/build-source/agent-<tag>.tgz`，拉下来和 `git show HEAD:<路径>`
   逐文件比。v31 的 65 个受控文件**和 HEAD 完全一致**
   （排掉 `deploy/demovideo/assets/` 那 200 多个测试素材，它们不在 git 里）。
   v29 就是这么查出来还差着 `server.py` 那 44 行的。
2. **比线上真发出来的字节。** 带 ID token 抓大屏页面，和本地 `HTML_PAGE`
   逐字节比——40143 : 40143，**完全一致**。上一次就是这么抓到 `ADK Web`
   还留在一句 HTML 注释里的：注释也会发到浏览器，view-source 看得见。

计时日志云上验过了：

```
2026-09-09T13:02:04  turn done session=warmup-v31 in 2.40s (first sentence 2.40s, 1 sentences)
```

容器侧 2.40s，客户端掐表 2.70s，差的 0.3s 是网络加平台。

**引擎换了实例，bilibili 的 session 已经重新暖过**（单 `f97a1e`，标题读到、
封面截到、ffprobe 直接打开了媒体轨，没撞 412）。那单停在「等确认」，不会自己跑。

### 2026-09-09 早些时候那次部署带上去的东西

### 2026-09-09 这次部署带上去的东西

| 改了什么 | 云上怎么验的 |
| :--- | :--- |
| 报告新增「判读情况」一行，`0 违规` 和 `没看清` 不再都发绿票 | 单 `292712`「7 项检查中 2 项无法判定」；单 `0b581f`「10 项检查中 5 项无法判定」 |
| `gs://` 目标不再追问时间段（问了也用不上） | 单 `292712`，用户原话原样发，一次到位「要稽核：整段视频，从头看到尾」，12.4s |
| 读意图超时改成**每次**算，试两次；超时不再说「读不懂」 | 只有单测。云上这几次都没超时，**没有云上证据** |
| 大屏加「这场稽核已于 xx:xx 结束了，没有实时画面」 | `/api/state` 上 `finished_at` 在 RUNNING 时是 `None`、COMPLETED 时盖上（单 `85be2c`、`0b581f` 各一次） |

**一个已知的坑，不是这次引入的**：大屏的房间状态在进程内存里，
**大屏一重新部署，所有历史单子的结果面板和结束提示一起清空**（变回 `IDLE`）。
上面 `85be2c` 的状态就是这么没的。要留痕得把房间状态外置，这次没做。

引擎这次是用 **`update-spec`** 部的，不是 `update-image`。
`update-image` 只改 `spec.container_spec.image_uri`，
**探针、并发数、环境变量都不会跟着上去**——这次的改动三样都动了。

---

## 你批的三件事，都做完了

### 1. 容器补日志 —— 修了，云上验过

`server.py` 从来没调过 `logging.basicConfig()`，所以稽核自己打的 INFO
一条都没进 Cloud Logging。这也是为什么之前查大屏问题只能靠猜。
现在日志走 **stdout**（stderr 会被 Cloud Run 一律标成 ERROR），
JSON 带 `severity`，在日志浏览器里是结构化条目。
上面那些 `preview pump` / `sched` / `Window N analysed` 的数字，
全是这条改动之后才看得到的。

### 2. 看门狗 —— 加了，在跑

一个单子超过 N 分钟不出新窗口就大声失败，不再挂在 `running` 上装活着。
日志里长这样：`Job 19c47f alive: 75s elapsed, 21 windows, 0s since the last one (allowance 300s)`。

### 3. `turn.py` 那条默认规则 —— 删了

原来是「没提时间 = start 0、end -1，`span_understood` 填 true」。
那等于替客户编了一个他没说过的时间段，还告诉下游「我听懂了」。
现在没提时间就回头问客户要。

---

## 大屏卡顿：根因、证据、修法

### 根因

**Cloud Run 在没有请求在处理的时候，不给这个实例 CPU。**

而我们的稽核是**故意甩出请求之外**跑的——这是 Phase 0 实测逼出来的唯一写法
（流式那条路上，容器里的活会在 `start + 900.0s` 一秒不差地被 cancel）。
于是：**让稽核活下来的那个决定，正好就是让它挨饿的原因。**

这不是推测，是量出来的。`/proc/self/schedstat` 第二个字段是
「可运行但没被调度上 CPU 的时间」：

| 阶段 | runqueue 等待 | 线程 tick（要 83ms） | 抓帧泵 | Chromium 投屏 |
| :--- | ---: | ---: | ---: | ---: |
| 没有请求（单 `4e38e2`） | **78.7% / 85.4% / 82.3%** | 100–200ms | 2.7–3.5 fps | 0.0–0.1 fps |
| 我在外面猛打请求 | **44.3% → 29.7%** | **83ms，p90 83ms** | 5.9 → 8.2 fps | **5.8 → 6.6 fps** |

同一个容器、同一场稽核，只差「外面有没有人在敲门」。

顺带解释了另一半：Chromium 只有收到 `Page.screencastFrameAck` 才推下一帧，
而 ack 是同一个事件循环发的。**循环挨饿 → ack 发得慢 → 投屏自己就降速了。**
所以「泵慢」和「投屏慢」不是两个毛病，是一件事。

### 修法

`deploymentSpec.keepAliveProbe` + 一个会**把响应挂住**的 `/is_busy`。

这个字段只在 API discovery document 里有
（`GoogleCloudAiplatformV1KeepAliveProbe{maxSeconds ≤ 3600, httpGet{port, path}}`），
runtime-contract 和 optimize-and-scale 两页文档**一个字都没提探针和 CPU 分配**
（这一轮又抓了一次，确认没有）。`/is_busy` 就是那个 schema 里的示例路径。

**正因为语义没文档，这个端点在稽核跑着的时候把响应挂住不返回**——
这样「有请求在飞」是构造出来的事实，不是我假设平台会怎么做。
挂多久由 `KEEPALIVE_HOLD_SECONDS` 控制（现在 25 秒，设 0 就是对照组）。

**并发必须 ≥ 2**：挂住的探针占掉一个槽，`container_concurrency=1` 的话
GE 的正常轮次会吃 429。所以 `deploy.py` 里改成了 `containerConcurrency: 2`
——「2 减掉探针那个」仍然是原来「一次只跑一轮真活」的保证，不是放宽。

### 修完之后（单 `19c47f`，v17，300 秒素材，全程开着观看端）

| | |
| :--- | :--- |
| runqueue 等待 | **0ms（0.0%）** ← 修之前是 79–85% |
| 线程 tick | 中位 83ms，p90 83ms，最坏 132ms（要的就是 83ms） |
| 抓帧泵 | **11.9/s（配的是 12）** |
| Chromium 投屏 | 12.4 fps |
| **观看端收到** | **1014 帧，11.9 fps，平均 12.5 KB，共 12.37 MB** |
| **帧间隔** | **中位 84ms，p90 92ms，最坏 122ms** |
| **超过 1 秒的卡顿** | **0 次** |
| 窗口 / 失败 | 25 / 0 |
| `start_audit` 返回 | 0.34s |
| 端到端 | **86.3s**（修之前同样的活要 233–498s） |

「最坏 122ms」这个数是回答你那句「不要再跟之前一样卡顿」的：
**整场一次都没有出现过让人等的静止画面。**

> 我之前对这个症状给过两个解释（在途上限、观看端轮询），
> **都被实测否掉了**，这里不再重提。

---

## 模型推理延迟（同一单 `19c47f`，25 个窗口）

| | |
| :--- | :--- |
| 每窗口 | 最快 6.4s，中位 **8.5s**，p90 11.6s，最慢 20.2s |
| 失败 | 0 |
| 输入 token | 3574/窗口，全场 89350 |
| 输出 token | 16423 |

25 个窗口 86 秒跑完，说明并发是真在并发（串行的话要 200 秒以上）。
和 09-04 那次比：每窗口延迟没变（本来就不是瓶颈），
**变的是窗口之间不再有大段空转**——那段空转就是容器在等 CPU。

---

## bilibili：跑通了

**你要求的验收靶子过了。** 上面那些大屏数字是在演示 HLS 上量的，
下面这一场是真的 bilibili。

单 `e40125`（v18，`BV1MisPzrE29`「第一视角体验霸王茶姬忙碌日常」，
从 01:00 起稽核 5 分钟，全程开着观看端）：

| | |
| :--- | :--- |
| preflight | `stream`（Plan A），519.3s，852x480，标题读到了 |
| 窗口 / 失败 | **25 / 0** |
| 违规 / 红线 | 4 / 0 |
| 覆盖 | 60.0 → 363.0（请求 60–360），`complete=True` |
| 结束原因 | 已采集到请求的终点 06:00 |
| 端到端 | 327s（采集 288.7s） |
| 每窗口输入 token | 10504 |

**这一场是在这台开发 VM 上本地跑的，不是在云上。**
原因是登录态：`auth/bilibili.json` 被 `.dockerignore` 和 `.gcloudignore` 挡在镜像外
（**这是对的**，里面是活的会话 cookie），容器里的 `AUTH_STATE_DIR=/tmp/auth` 是空的。
所以**云上这条引擎现在还进不去 bilibili**，要么给它挂 Secret Manager，
要么演示就用本地这条路。**代码是同一份**，卡顿那个 bug 和它的修法两边一模一样。

顺带记两件这场跑出来的事：

- **第一次导航吃了 HTTP 412 风控**，交给 Computer Use 兜底，进去了。这条路是通的，
  但它花钱也花时间，演示前最好先手动开一次页面把风控养熟。
- **有一个窗口跑了 250.7 秒**（其余 24 个都在 15–25 秒，中位 19.9s）。
  日志里没有重试、没有报错，就是那一次模型调用自己慢。
  它不影响结果（25/25 都回来了），但**它把整场的墙上时间拖长了三分半**——
  演示的时候可能会出现「最后一个窗口迟迟不回来」。这个还没查。

---

## 你报的那个：弹登录框之后大屏不动了

**你的观察全对，而且你顺手说的那半句才是关键——「分段巡检还在正常跑」。**
那正是这个 bug 的形状：稽核是好的，只有画面死了。

### 是什么

Plan A（抓流）下，ffmpeg 直接去拉媒体地址，**浏览器那个页面唯一的作用就是给你看**。
bilibili 大约在播放开始 70 秒后弹登录框，**并且把视频暂停**。
Chromium 的投屏（screencast）**只在页面重绘的时候才吐帧**——画面不动，就一帧都不吐。
而抓帧泵手里攥着上一帧，照样按 11.9 fps 往大屏发。

于是：大屏收到的帧率是满的、字节数是恒定的、每一帧长得一模一样。
**所有指标都显示健康，只有画面是死的。** 上一场 `692b53` 的日志：

| 时刻 | 投屏 | chrome CPU | 泵/发出 | 每帧大小 |
| :--- | ---: | ---: | :--- | ---: |
| 04:02:16 | 30.1 fps | 220% | 11.9 / 11.9 | 62.7 KB |
| 04:03:02 | 8.1 fps | 69% | 11.9 / 11.9 | 78.8 KB |
| 04:03:17 → 结束 | **0.0 fps** | **2%** | 11.9 / 11.9 | **84.3 KB（一动不动）** |

### 根子在哪：三处，同一个辐射范围

1. **Plan A 从来不开页面看门狗**（`pipeline.py`）。看门狗才是那个「定期关弹窗、
   把播放踹起来」的东西。原来的判断是「录屏才需要管页面」——**对采集是对的，
   对大屏是错的**。
2. **Plan A 从来不记 `_player_rect`**，所以就算关掉了弹窗，
   播放器被踢出网页全屏之后也没人把它放回去，剩下的时间你看的是角落里一张邮票。
3. **画面死了没有任何人吭声**。日志里没有，大屏上没有，报告里也没有。

### 改了什么

| 改动 | 在哪 |
| :--- | :--- |
| 两条路都开看门狗，**但只有 Plan B 有权因为页面而结束稽核** | `pipeline.py:_watch_page(page_is_source=...)` |
| Plan A 不设「三次恢复就放弃」的上限——它没有什么结论要下，弹窗每隔几分钟来一次，放弃了就等于把后面五十分钟的画面让出去 | 同上 |
| Plan A 也记 `_player_rect`，弹窗过后把播放器放回全屏 | `pipeline.py:_build_producer` |
| **关掉弹窗之后顺手把播放踹起来**（关框只是一半，bilibili 是连着暂停的） | `navigator/bilibili.py:keep_clear` |
| 关弹窗的日志 DEBUG → INFO。DEBUG 上不了 Cloud Logging，于是「没见过弹窗」和「关了但播放没起来」在日志里长得一样——这是两个不同的 bug | `navigator/bilibili.py:_dismiss_overlays` |
| 投屏整整 15 秒一帧都没有就告警 | `capture/preview.py:_pump_report` |
| 画面冻住了在大屏动作栏说一声，**只说一次**，恢复了自动复位 | `pipeline.py:_note_picture_frozen` |
| 云上这条路终于接上 `on_status`（本地 `adk web` 一直有，云上从来没有，事件到日志为止） | `audit_service.py:_announce` |

**有一条是刻意反着写的**：Plan A 下页面卡住**不许**结束稽核。
写反了的话，bilibili 一弹框就会把一场好好的稽核在 01:19 掐掉——
请求的是 5 分钟。单测里专门钉了这条。

### 怎么证明修好了

同一个视频、同一个位置、开着观看端跑 5 分钟（单 `e40125`）：

```
04:49:07  Dismissed overlay .bili-mini-mask .bili-mini-close-icon
04:49:07  Dismissed overlay .bili-mini-close-icon
04:49:07  Hid 1 stubborn overlay element(s)
```

**登录框照常在 70 秒那里弹出来了**（播放 04:47:57 开始），被关掉了，
前后两条投屏日志分别是 24.9 fps 和 24.6 fps——**没有掉到 0**。

观看端这一侧：

| | |
| :--- | :--- |
| 收到 | 3416 帧 / 288s = **11.9 fps** |
| 帧间隔 | 中位 84ms，最坏 127ms |
| 超过 1 秒的卡顿 | **0 次** |
| 每帧大小 | 平均 64.3 KB，范围 **7.5–120.4 KB**（坏的时候是恒定 84.3 KB） |
| **不重样的画面** | **3416 帧里有 3381 张不一样** |
| **最长的一段静止** | **1.1 秒**（坏的时候是两分钟） |

「不重样的画面」和「最长静止」是这次专门加的量法：
坏的时候帧率和字节数都是好看的，**只有「这一帧和上一帧是不是同一张」会说实话。**

**没动过的那条路也验了**：Plan B 在云上重跑一遍演示视频（单 `1c8257`，v18），
10 个窗口 0 失败、`complete=True`、结束原因是「已采集到请求的终点」而不是卡住或播完，
178.4 秒跑完。看门狗那段代码是重写过的，所以这条必须单独证明。

---

## 需要人处理的遗留

按重要性排：

1. **给云上的容器一条进 bilibili 的路**（Secret Manager 挂 `storage_state`）。
   本地这条已经通了，云上还没有。
2. **在 GE 界面里从头走一遍**：`@门店视频稽核` → 点 chip、粘网址 → 看它复述确认
   → 回「确认」→ 等结果 → 点大屏链接。**必须 @**，不 @ 会被 GE 自带助手接走。
3. **在笔记本上开大屏**：
   `gcloud run services proxy cctv-monitor --project=study-project-496907 --region=us-central1`
   → `http://localhost:8080/?job=<单号>`。
4. **删掉我误建的那个引擎**（我删被权限拦下了）：
   ```
   .venv/bin/python deploy/agent_runtime/deploy.py delete \
     projects/596821501265/locations/us-central1/reasoningEngines/7288747792678256640
   ```
   在用的是 `6844158066963775488`，别删错。
5. **要不要开 IAP**：brand 在（`projects/596821501265/brands/596821501265`），
   底下还没有 OAuth 客户端，建它需要你自己来。开了之后浏览器直接点链接就能进。
6. Firestore 里几个测试单还挂着状态：`d7680d`、`f500e5`、`c70488`，
   外加这两天的 `8ff781` / `4e38e2` / `6c2e8d` / `60caa7` / `19c47f` /
   `692b53` / `e40125` / `1c8257`。
7. `cctv_audit/checkpoints/audit_records.jsonl` 里 3 条测试记录删不删，你定
   （里面有测试跑的证据帧，我不单方面删）。
8. 提交和 PR 你自己来。

---

## 演示前你要知道的一件事（没变）

**演示素材上每一个窗口的判定都是 `CANNOT_DETERMINE`。**
那是 ffmpeg 合成的色块和时钟，里面没有任何可判定的东西。
**链路会跑得很漂亮，但报告不会给出一个像样的合规结论。**
想让报告有内容，把一段真实门店录像喂给
`./deploy/demovideo/make_assets.sh /path/to/real.mp4` 重新出素材再部署。

## Bug 4 —— 这次没复现

原来记的是：单 `f500e5` 的 preflight 判 `stream`，实际跑成了 `screen`，
猜是媒体地址要 Referer / Cookie。

这次 `e40125` 从头到尾都是 `stream`：preflight 判 `stream`，
采集开始又判一次还是 `stream`，报告里 `capture_mode=stream`。**没有回落。**
所以那次要么是登录态不同（这次带了 `auth/bilibili.json`），
要么是当时那条媒体地址本身有问题。**没复现不等于修好了**，
下次再看到就照这个对照着查。

## 一个配置上的出入（没变）

引擎上 `DEFAULT_SOP_ID=chagee-store-v1`。计划里写的是**留空**，理由是
「配了它，客户不说版本号也能跑，跑出来的报告按的是部署时定的那版——那是替客户选版本」。
现在配着，是为了 chip 里那句话能直接跑通。**演示可以，交付给客户前建议清掉。**

## 测试

`.venv/bin/python -m pytest -q` —— **380 passed**
（328 → 364：CPU/调度探针一组，`/is_busy` 挂起行为一组，
外加一条断言部署 spec 里探针指向 `/is_busy:8080` 且并发 ≥ 2。
364 → 380 是这一轮的画面冻结：Plan A 看门狗 5 条
（**其中一条专门钉住「页面卡住不许结束稽核」**，写反了会把 5 分钟的活在 01:19 掐掉）、
关弹窗顺带恢复播放 4 条、投屏静默告警 2 条、大屏动作栏播报 5 条）。
